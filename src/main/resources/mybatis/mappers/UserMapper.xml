<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.blog.repository.mybatis.mapper.user.UserMapper">
    <insert id="insert">
        insert into users (id, username, email, password, bio, image) values(
        #{user.id},
        #{user.username},
        #{user.email},
        #{user.password},
        #{user.bio},
        #{user.image}
        )
    </insert>

    <select id="findByEmail" resultMap="user">
        select id, username, email, password, bio, image from users where email = #{email}
    </select>

    <select id="findById" resultMap="user">
        select id, username, email, password, bio, image from users where id = #{id}
    </select>

    <resultMap id="user" type="com.example.blog.domain.user.User" >
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="bio" property="bio"/>
        <result column="image" property="image"/>
    </resultMap>
</mapper>